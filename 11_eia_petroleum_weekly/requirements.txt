pyyaml>=6.0
# PyYAML is the only pip dependency (reads config.example.yaml).
#
# Time series: EIA bulk PET.zip parsed with the standard library (zipfile/json/urllib).
# Report text: PDF -> text via the `pdftotext` CLI (poppler-utils), called through
#   subprocess. Install it with:  brew install poppler   (macOS)
#                                  apt-get install poppler-utils   (Debian/Ubuntu)
# The build checks for pdftotext and exits with a clear message if it is missing.
